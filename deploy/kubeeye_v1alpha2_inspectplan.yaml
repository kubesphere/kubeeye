apiVersion: kubeeye.kubesphere.io/v1alpha2
kind: InspectPlan
metadata:
  name: inspectplan
spec:
  maxTasks: 10
  ruleNames:
    - name: resourcesuse
    - name: noderesourceusetop
    - name: nodeinfo
    - name: apistatus
  timeout: 30m

