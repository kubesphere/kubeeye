---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: inspectrules.kubeeye.kubesphere.io
spec:
  group: kubeeye.kubesphere.io
  names:
    kind: InspectRules
    listKind: InspectRulesList
    plural: inspectrules
    singular: inspectrules
  scope: Namespaced
  versions:
  - name: v1alpha2
    schema:
      openAPIV3Schema:
        description: InspectRules is the Schema for the InspectRules API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: InspectRulesSpec defines the desired state of InspectRules
            properties:
              prometheusEndpoint:
                type: string
              rules:
                items:
                  properties:
                    desc:
                      type: string
                    opa:
                      type: string
                    priority:
                      type: string
                    prometheus:
                      type: string
                    prometheusEndpoint:
                      type: string
                    ruleName:
                      type: string
                    tags:
                      items:
                        type: string
                      type: array
                  type: object
                minItems: 1
                type: array
            type: object
          status:
            description: InspectRulesStatus defines the observed state of InspectRules
            properties:
              importTime:
                format: date-time
                type: string
              state:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
